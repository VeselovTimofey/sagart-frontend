import type { SxProps } from '@mui/material';

const categories: SxProps = {
  display: 'grid',
  gridTemplateColumns: 'repeat(3, auto)',
  gap: '24px',
  paddingX: '3.75rem',
};

export default { categories };
